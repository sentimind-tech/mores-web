export type TInsight = {
    id: number,
    title: string,
    published_date: string,
    read_time: string,
    content: string,
    summary: string,
    pdf_file: string,
    button_image: string,
    slug: string,
}