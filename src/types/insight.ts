export type TInsight = {
    id: string,
    title: string,
    published_date: string,
    read_time: string,
    content: string,
    summary: string,
    pdf_file: string,
    button_image: string[],
}